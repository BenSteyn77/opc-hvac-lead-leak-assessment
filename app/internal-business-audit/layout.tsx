export const metadata = {
  title: "Internal Complete Business Audit | OPC",
  robots: { index: false, follow: false },
};

export default function InternalAuditLayout({ children }: { children: React.ReactNode }) {
  return children;
}
